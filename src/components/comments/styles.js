import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {},
  textArea: {
    height: 100,
    borderColor: 'gray',
    borderWidth: 1,
    padding: 5,
    marginBottom: 20,
  },
});

export default styles;
