import {
  StyleSheet,
} from 'react-native';

export default StyleSheet.create({
  touchableOpacity: {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'flex-end',
  },
  badgeText: {
    color: 'orange',
  },
  badgeContainer: {
    backgroundColor: '#333',
  },
});
