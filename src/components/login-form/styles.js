import React from 'react';
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    display: 'flex'
  },
  fieldContainer: {
    display: 'flex'
  },
  buttonContainer: {
    display: 'flex',
    alignItems: 'center'
  }
});
